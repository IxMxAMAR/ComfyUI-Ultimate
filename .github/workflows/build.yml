name: build

on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:

concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

env:
  IMAGE: ixmxamar/comfyui-ultimate

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 350
    steps:
      - uses: actions/checkout@v4

      - name: Free disk space
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: true
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: false
          swap-storage: true

      - name: Relocate Docker storage to /mnt (~70GB) for the ~20GB image + cache
        run: |
          sudo systemctl stop docker docker.socket || true
          sudo mkdir -p /mnt/docker
          echo '{ "data-root": "/mnt/docker" }' | sudo tee /etc/docker/daemon.json
          sudo systemctl start docker
          docker info | grep -i "docker root dir" || true
          df -h / /mnt || true

      - name: Add swap (prevents nvcc OOM during the SageAttention CUDA compile)
        run: |
          sudo swapoff -a || true
          sudo rm -f /mnt/swapfile || true
          sudo fallocate -l 20G /mnt/swapfile || sudo dd if=/dev/zero of=/mnt/swapfile bs=1M count=20480
          sudo chmod 600 /mnt/swapfile
          sudo mkswap /mnt/swapfile
          sudo swapon /mnt/swapfile
          free -h

      - uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USER }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # smoke_test.py + pip check run INSIDE the build (Dockerfile step 11);
      # a constraints/ABI regression therefore fails this build step.
      - name: Build image (load locally)
        uses: docker/build-push-action@v6
        with:
          context: .
          load: true
          tags: ${{ env.IMAGE }}:ci-${{ github.sha }}
          provenance: false

      - name: Node-import gate (all 29 packs must import, OFFLINE)
        # --entrypoint bash overrides the image ENTRYPOINT (start.sh) so we run the
        # gate script (not launch full ComfyUI without --cpu).
        # --network none: fail the build if ANY node needs the network to import
        # (e.g. import-time downloads like UltimateSDUpscale's USDU script) — those
        # must be vendored at build time so pods boot reliably offline.
        run: docker run --rm --network none --entrypoint bash ${{ env.IMAGE }}:ci-${{ github.sha }} /opt/scripts/ci_node_import_gate.sh

      - name: Tag + push (only on push / dispatch, after gates pass)
        if: github.event_name != 'pull_request'
        run: |
          SHA=$(echo "${{ github.sha }}" | cut -c1-12)
          docker tag ${{ env.IMAGE }}:ci-${{ github.sha }} ${{ env.IMAGE }}:latest
          docker tag ${{ env.IMAGE }}:ci-${{ github.sha }} ${{ env.IMAGE }}:$SHA
          docker tag ${{ env.IMAGE }}:ci-${{ github.sha }} ${{ env.IMAGE }}:cu128-torch2.8.0
          docker push ${{ env.IMAGE }}:latest
          docker push ${{ env.IMAGE }}:$SHA
          docker push ${{ env.IMAGE }}:cu128-torch2.8.0
