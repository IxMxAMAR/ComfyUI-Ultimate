# =============================================================================
# constraints.txt — ComfyUI Ultimate (cu128 / py3.12 / sm_120 Blackwell)
# Applied to EVERY pip/uv install via PIP_CONSTRAINT / UV_CONSTRAINT.
# Bounds versions only; never installs. ABI-sensitive packages are == hard-pinned.
# Baseline generated 2026-06-14. Re-verify vs live cu128 index on any bump.
# =============================================================================

# --- PyTorch triple (cu128, Blackwell sm_120). torchvision 0.23.0 hard-pins
#     torch==2.8.0, so this triple is mutually locked. Installed from
#     download.pytorch.org/whl/cu128 (see Dockerfile). ---
torch==2.8.0+cu128
torchvision==0.23.0+cu128
torchaudio==2.8.0+cu128

# --- Triton paired with torch 2.8.0 (auto-pulled by the torch cu128 wheel). ---
triton==3.4.0

# --- numpy 2.x + its compiled gatekeepers (numba is the numpy-2.x pinch point) ---
numpy==2.2.6
numba==0.61.2
llvmlite==0.44.0

# --- opencv: the single winner. Other variants are uninstalled and this is
#     reinstalled in the final normalization layer (Dockerfile). ---
opencv-contrib-python-headless==4.11.0.86

# --- onnxruntime: the single GPU winner (cuDNN 9 / CUDA 12.x). ---
onnxruntime-gpu==1.22.0

# --- HuggingFace set: coherent group, move together on any bump. ---
transformers==4.53.2
tokenizers==0.21.2
huggingface-hub==0.34.3
diffusers==0.34.0
accelerate==1.8.1
peft==0.16.0
safetensors==0.5.3

# --- protobuf under RMBG <6 cap, above mediapipe floor ---
protobuf==4.25.8
# mediapipe 0.10.30+ dropped the numpy<2 cap (recon's 0.10.21 would hard-fail vs numpy 2.2.6)
mediapipe==0.10.35
pillow==11.3.0
simpleeval==1.0.3

# --- other cross-cutting pins (bare/floor demands; pinned for reproducibility) ---
scipy==1.15.3
scikit-image==0.25.2
scikit-learn==1.6.1
kornia==0.8.1
timm==1.0.16
sentencepiece==0.2.0
einops==0.8.1
matplotlib==3.10.3
clip-interrogator==0.6.0
open-clip-torch==2.32.0
gguf==0.17.1
ultralytics==8.3.162
spandrel==0.4.1
